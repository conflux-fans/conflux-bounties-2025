# =============================================================================
# AI Smart Contract Auditor - Environment Configuration
# =============================================================================
# Copy this file to .env.local and configure with your actual values
# See README.md for detailed setup instructions

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# AI API Keys (You need AT LEAST ONE of these)
# Get OpenAI key: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
# Get Anthropic key: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# Conflux Network API (Required for contract source code retrieval)
# This is the API endpoint - no key needed for public contracts
CONFLUXSCAN_API_KEY=https://evmapi.confluxscan.org
# Optional: If you have a premium ConfluxScan API key, add it here
# CONFLUXSCAN_API_KEY=your-premium-conflux-scan-api-key

# Database Configuration (Required)
# For local development with Docker (recommended):
DATABASE_URL="postgresql://postgres:mypassword123@localhost:5555/audit_db?schema=public"
# For local PostgreSQL installation:
# DATABASE_URL="postgresql://username:password@localhost:5432/audit_db?schema=public"
# For Vercel deployment: This will be auto-populated by Vercel Postgres

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Application Environment
NODE_ENV=development
PORT=3000
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Security Keys (Optional but recommended for production)
# Generate random strings for these:
JWT_SECRET=your-random-jwt-secret-256-bits-long
WEBHOOK_SECRET=your-webhook-hmac-secret-key

# Rate Limiting (Optional)
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_WEBHOOK_REQUESTS_PER_MINUTE=30

# Monitoring (Optional)
# SENTRY_DSN=your-sentry-dsn-for-error-tracking

# =============================================================================
# DOCKER-SPECIFIC SETTINGS (Only needed for Docker deployment)
# =============================================================================

# Redis Cache (Optional - only needed for Docker deployment)
# REDIS_URL=redis://redis:6379

# Vector Database (Optional - only needed for advanced features)
# VECTOR_DB_URL=postgres://vector_user:vector_pass@vector-db:5432/vector_db

# Docker Compose Project Name
# COMPOSE_PROJECT_NAME=smart-contract-auditor

# =============================================================================
# QUICK START GUIDE
# =============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Get an OpenAI API key from: https://platform.openai.com/account/api-keys
# 3. Add your OpenAI key to OPENAI_API_KEY above
# 4. Start database: docker-compose up -d
# 5. Run migrations: npm run db:migrate
# 6. Start app: npm run dev
# 7. Visit: http://localhost:3000
#
# For detailed setup instructions, see README.md
